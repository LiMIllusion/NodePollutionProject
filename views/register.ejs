
<!DOCTYPE html>
<html lang="en">
<head>
    <%-include('./partials/head')%>
</head>
<body>
    <%-include('./partials/navbar')%>
    <div class="container py-3">
        <div class="card" style="opacity: 4;">
        <img src="" alt="">
        <div class="card-body">
            <h5 class="card-title py-2">
                Registrati
            </h5>
            <div class="col">    
                <div class="card-text">
                    <%-include('./partials/messages')%>
                    <form  action="/users/register" method="POST">
                      <div class="form-group">    
                        <label for="name">Username</label>
                        <input
                          class="form-control"
                          type="name"
                          name="name"
                          placeholder="Nome utente"
                          value="<%= typeof name != 'undefined' ? name : '' %>"
                        />
                      </div>
                        <div class="form-group">
                            <label for="email">Indirizzo email</label>
                            <input type="email" class="form-control" name="email" placeholder="mariorossi@mail.com" value="<%= typeof email != 'undefined' ? email : '' %>">
                        </div>
                        <div class="form-group">
                            <label for="password">Password</label>
                            <input class="form-control" id="password" name="password" type="password"
                            value="<%= typeof password != 'undefined' ? password : '' %>">
                        </div>
                        <div class="form-group">
                          <label for="password2">Confirm password</label>
                          <input class="form-control" id="password2" name="password2" type="password"
                          value="<%= typeof password2 != 'undefined' ? password2 : '' %>">
                      </div>
                        <button type="submit" class="btn btn-primary btn-block green-custom">Submit</button>
                    </form>
                    <p >Sei già registrato? <a href="/users/login" class="green-custom-text">Accedi</a></p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>

